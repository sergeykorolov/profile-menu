{
  "name": "rockstone-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "homepage": "https://sergeykorolov.github.io/profile-menu",
  "scripts": {
    "dev": "parcel index.html",
    "build": "parcel build index.html --public-url /profile-menu/",
    "test": "echo \"Error: no test specified\" && exit 1",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-less": "^2.0.0-rc.0",
    "parcel-bundler": "^1.12.5",
    "typescript": "^4.4.3"
  }
}
